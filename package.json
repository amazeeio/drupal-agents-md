{
  "name": "drupal-agents-md",
  "private": true,
  "description": "AI Agent Development Guides for Drupal",
  "scripts": {
    "lint": "markdownlint '**/*.md'",
    "lint:fix": "markdownlint --fix '**/*.md' && prettier --write '**/*.md'",
    "format": "prettier --write '**/*.md'",
    "check": "markdownlint '**/*.md' && prettier --check '**/*.md'"
  },
  "devDependencies": {
    "markdownlint-cli": "^0.44.0",
    "prettier": "^3.5.0"
  }
}
